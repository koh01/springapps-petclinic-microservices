spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/koh01/springapps-petclinic-microservices-config
        native:
          search-locations: classpath:.
  profiles:
    active: native
